package edu.gatech.gtevents.gtevents.loginscreen;

/**
 * Verifies that the credentials are correct.
 */
public class CredentialVerifier {

    /**
     * Verifies that a pair of GT credentials is correct and valid.
     * @return true if the credentials were valid.
     */
    public static boolean verify(String username, String password) {
        // TODO (acalabrese): I need to implement this feature and see hwo GT API handles it.
        return true;
    }
}
